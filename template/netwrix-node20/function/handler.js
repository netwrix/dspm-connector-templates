'use strict'

module.exports = async (event, context) => {
  // Example implementation - replace with actual handler logic
  return {
    statusCode: 200,
    body: { message: "Hello from Netwrix Node20 handler" }
  }
}
